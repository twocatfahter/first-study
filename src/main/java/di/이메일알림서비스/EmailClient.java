package di.이메일알림서비스;

public class EmailClient {

    public void sendEmail(String email, String message) {
        // 실제로 이메일 서버에 연결해서 메일을 보내는 로직이 필요합니다.
        System.out.println("Email sent to: " + email + " with Message=" + message);
    }
}
